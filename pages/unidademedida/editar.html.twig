{% extends 'layouts/dashboard.html.twig' %}


{% block title %}| Unidade de Medida | Cadastrar{% endblock %}
{% block stylesheet %}
    <link rel="stylesheet" href="/dist/css/iziToast.min.css">
{% endblock %}

{% block breadcrumb %}
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
            </div>
        </div><!-- /.container-fluid -->
    </section>

{% endblock %}


{% block content %}
    <section class="content">

        <div class="row">
            <div class="col-12">

                <div class="card card-primary">
                    <!-- /.box-header -->
                    <!-- form start -->
                    <form role="form" action="/unidademedida/salvareditar" method="post" class="jsonForm">
                        <div class="card-body">
                            <div class="form-group row">
                                <h3>Editar Unidade de Medida</h3>
                                <div class="form-group col">
                                    <a role="button" href="/unidademedida/" class="btn btn-info"   ><span class="fas fa-list"></span></a>
                                    <a role="button" href="/unidademedida/formCadastrar" class="btn btn-success"><span class="fas fa-user-plus"></span></a> 
                                </div>
                            </div>
                            <div class="form-group row">
                                <div class="form-group col-md-1">
                                    <label for="id_unidademedida">Código</label>
                                    <input type="number" class="form-control" id="id_unidademedida" name="id_unidademedida" readonly value="{{ linha['id_unidademedida'] }}">
                                </div>
                                <div class="form-group col">
                                    <label for="nome">Unida de Medida</label>
                                    <input type="text" class="form-control" id="unidademedida" name="unidademedida" placeholder="Editar Unidade de medida" required value="{{ linha['unidademedida'] }}">
                                </div>
                            </div>
                        </div>
                        <!-- /.box-body -->

                        <div class="card-footer">
                            <button type="submit" class="btn btn-primary">Salvar</button>
                        </div>
                    </form>
                </div>

            </div>
        </div>
        <!-- /.row -->
    </section>

{% endblock %}



{% block javascriptPlugins %}
    <script src="/dist/js/jquery.form.js"></script>
    <script src="/dist/js/iziToast.min.js"></script>
{% endblock %}

{% block jquery %}
    <script>
        $(document).ready(function(){
            $('.jsonForm').ajaxForm({
                dataType:  'json',

                success:   function(d){
                    if (d.status==0){
                        iziToast.error({
                            message: d.mensagem
                        });
                    }else{
                        iziToast.success({
                            message: d.mensagem,
                            onClosed: function(){
                                document.location = "/unidademedida";
                            }
                        });

                    }
                }

            });
        });
    </script>
{% endblock %}

{% block javascript %}
{% endblock %}
